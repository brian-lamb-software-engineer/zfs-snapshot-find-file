# Copy this file to .env and update values for your environment.
# NOTE: This example contains placeholders only. Do NOT commit personal
# dataset paths into the repository. Create a local `.env` and add it to
# your global gitignore if you need to persist personal configuration.
# This file is ignored by git when present as .env (see .gitignore).

# These variables are used by the TESTS to invoke the tool. Example values:
#   SFF_DATASET="/<pool>/<dataset>/<path>"          # e.g. /tank/projects/site
#   SFF_SNAPSHOT_PATTERN="*"                        # glob or pattern for snapshot names your searching against
#   SFF_FILE_PATTERN="*.html"                       # file glob to search for, if indicating specific files, or wildcard all.
#
# Example full command assembled by the tests:
#   ./snapshots-find-file -cv -d "$SFF_DATASET" --clean-snapshots -s "$SFF_SNAPSHOT_PATTERN" -f "$SFF_FILE_PATTERN"

SFF_DATASET="/<pool>/<dataset>/<path>"
SFF_SNAPSHOT_PATTERN="*"
SFF_FILE_PATTERN="*"
